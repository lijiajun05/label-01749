spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/class_manage?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: classmanage
    password: classmanage123
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# 开发环境日志级别
logging:
  level:
    com.classmanage: debug
    org.hibernate.SQL: debug
